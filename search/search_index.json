{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Welcome to the NL2SQL Platform","text":"<p>The NL2SQL Platform is an enterprise-grade engine for converting natural language into SQL.</p> <p>It features:</p> <ul> <li>Defense-in-Depth Security (RBAC, Read-Only enforcement).</li> <li>Multi-Database Routing (Federated queries across Postgres, MySQL, etc.).</li> <li>Agentic Reasoning (Iterative planning and self-correction).</li> </ul>"},{"location":"#quick-start","title":"Quick Start","text":"<pre><code># Install Dependencies\npip install -e packages/core\n\n# Run a Query\npython -m nl2sql.cli --query \"Show me top 5 users\"\n</code></pre> <p>Get Started Guide</p>"},{"location":"#documentation-structure","title":"Documentation Structure","text":""},{"location":"#architecture","title":"Architecture","text":"<p>Understand the \"Map-Reduce\" design, the SQL Agent pipeline, and how the Physical Validator ensures safety.</p>"},{"location":"#guides","title":"Guides","text":"<p>Step-by-step instructions for installation, configuration, and deploying to production with Docker.</p>"},{"location":"#reference","title":"Reference","text":"<p>Technical specifications for the CLI, API, and internal Node logic.</p>"},{"location":"architecture/adapters/","title":"Adapters (Plugins)","text":"<p>Connectivity is strictly decoupled from the Core engine. The Core knows nothing about specific SQL drivers, connection strings, or dialect quirks.</p>"},{"location":"architecture/adapters/#the-architecture","title":"The Architecture","text":"<p>We use Python's <code>importlib.metadata</code> entry points to discover and load adapters dynamically.</p> <pre><code>sequenceDiagram\n    participant Core\n    participant Registry\n    participant Plugin as PostgresAdapter\n\n    Core-&gt;&gt;Registry: get_adapter(\"postgres\")\n    Registry-&gt;&gt;Plugin: Load Entry Point\n    Plugin--&gt;&gt;Registry: Return Adapter Instance\n\n\n    Plugin--&gt;&gt;Core: { dry_run: true, limit: true }\n\n    Core-&gt;&gt;Plugin: execute(\"SELECT 1\")\n    Plugin--&gt;&gt;Core: Result(rows=[(1)])</code></pre>"},{"location":"architecture/adapters/#the-interface","title":"The Interface","text":"<p>Every adapter adheres to the contract defined in <code>nl2sql-adapter-sdk</code>.</p> Method Purpose <p>| <code>fetch_schema()</code> | Returns <code>SchemaMetadata</code> (Tables, Columns, Types). | | <code>execute(sql)</code> | Runs the query and returns normalized <code>QueryResult</code>. | | <code>cost_estimate(sql)</code> | Returns estimated rows/cost (optional). |</p>"},{"location":"architecture/adapters/#creating-a-new-adapter","title":"Creating a New Adapter","text":"<ol> <li>Create a new python package (e.g. <code>nl2sql-oracle</code>).</li> <li>Implement <code>DatasourceAdapter</code>.</li> <li>Register the entry point in <code>pyproject.toml</code>:</li> </ol> <pre><code>[project.entry-points.\"nl2sql.adapters\"]\noracle = \"nl2sql_oracle.adapter:OracleAdapter\"\n</code></pre>"},{"location":"architecture/indexing/","title":"Data Indexing Strategy","text":"<p>Objective: Move beyond schema-only understanding by indexing structured knowledge about data. This improves routing accuracy, prevents hallucinations, and enhances SQL planning.</p>"},{"location":"architecture/indexing/#the-problem-data-blindness","title":"The Problem: Data Blindness","text":"<p>Without indexing, the AI knows the tables but not the data.</p> <ol> <li>Hallucinations: Asking for \"Gold Tier\" users when the data uses <code>tier='premium_level_1'</code>.</li> <li>Inefficient Routing: Searching legacy tables for recent data.</li> <li>Invalid Assumptions: Querying NULL columns.</li> </ol>"},{"location":"architecture/indexing/#layered-indexing-model","title":"Layered Indexing Model","text":"<p>We use a layered approach to balance cost and accuracy.</p>"},{"location":"architecture/indexing/#layer-1-statistical-index-must-have","title":"Layer 1: Statistical Index (Must Have)","text":"<p>Stores metadata about the data, not the data itself.</p> <ul> <li>Row counts</li> <li>Distinct values</li> <li>Null percentages</li> <li>Min/Max sets (e.g. date ranges)</li> </ul> <p>Benefit: Helps the Planner know if a query will return 0 rows before writing SQL.</p>"},{"location":"architecture/indexing/#layer-2-sample-data-controlle","title":"Layer 2: Sample Data (Controlle)","text":"<p>Stores a small, stratified sample of actual rows (e.g. 100 rows).</p> <ul> <li>Security: PII is masked.</li> <li>Usage: Used for few-shot prompting in the Generator.</li> </ul>"},{"location":"architecture/indexing/#layer-3-business-entity-index","title":"Layer 3: Business Entity Index","text":"<p>Maps business terms to database realities.</p> <ul> <li>\"Client\" -&gt; <code>customers</code> table.</li> <li>\"Revenue\" -&gt; <code>total_amount</code> column.</li> <li>Storage: Vector embeddings for semantic search.</li> </ul>"},{"location":"architecture/indexing/#retrieval-strategy","title":"Retrieval Strategy","text":"<p>When a query arrives, the Decomposer and Planner retrieve context in this order:</p> <ol> <li>Schema Context: Table definitions.</li> <li>Entity Index: Mapping synonyms.</li> <li>Statistical Profile: Validating values.</li> <li>Sample Data: Only if needed for complex join logic.</li> </ol>"},{"location":"architecture/indexing/#security-privacy","title":"Security &amp; Privacy","text":"<p>[!IMPORTANT] We NEVER index full raw data blindly.</p> <ul> <li>PII: Automatically masked or excluded.</li> <li>Financials: Sensitive identifiers are hashed.</li> <li>Tenant Isolation: Separate indexes for separate logical tenants.</li> </ul>"},{"location":"architecture/overview/","title":"System Overview","text":"<p>The NL2SQL Platform is a modular, agentic system designed to convert natural language questions into accurate, authorized SQL queries across multiple database engines.</p>"},{"location":"architecture/overview/#core-philosophy","title":"Core Philosophy","text":"<p>The system is built on three pillars:</p> <ol> <li>Agentic Workflow: Instead of a \"one-shot\" generation, we use a multi-step SQL Agent that plans, validates, and refines queries.</li> <li>Map-Reduce Routing: Complex queries are decomposed and routed to the most relevant datasources (Map), then executed in parallel, and finally aggregated (Reduce).</li> <li>Plugin Architecture: The core engine is database-agnostic. Connectivity is handled by \"Adapters\" (Plugins) that strictly implement the <code>nl2sql-adapter-sdk</code>.</li> </ol>"},{"location":"architecture/overview/#high-level-architecture","title":"High-Level Architecture","text":"<p>The system follows a directed graph execution flow, orchestrating several specialized AI Agents.</p> <pre><code>graph TD\n    UserQuery[\"User Query\"] --&gt; Semantic[\"Semantic Analysis\"]\n    Semantic --&gt; Decomposer[\"Decomposer Node\"]\n    Decomposer -- \"Splits Query\" --&gt; MapBranching[\"Fan Out (Map)\"]\n\n    subgraph Execution_Layer [\"Execution Layer (Parallel)\"]\n        MapBranching --&gt; SQL_Agent[\"SQL Agent (Planner + Validator + Executor)\"]\n    end\n\n    SQL_Agent -- \"Result Set\" --&gt; Reducer[\"State Aggregation\"]\n    Reducer --&gt; Aggregator[\"Aggregator Node\"]\n    Aggregator --&gt; FinalAnswer[\"Final Response\"]</code></pre>"},{"location":"architecture/overview/#1-semantic-analysis-decomposition","title":"1. Semantic Analysis &amp; Decomposition","text":"<p>The entry point. It understands the user's intent (\"Compare sales in US and EU\") and identifies which datasources contain the relevant data. If necessary, it splits the query into sub-questions (e.g., \"Get US sales\", \"Get EU sales\").</p>"},{"location":"architecture/overview/#2-the-sql-agent","title":"2. The SQL Agent","text":"<p>For each relevant datasource, a dedicated SQL Agent is spawned. This is where the core logic lives. It iteratively plans a query, validates it against security policies, generates SQL, and verifies execution feasibility.</p> <p>Read more about the SQL Agent.</p>"},{"location":"architecture/overview/#3-aggregation-reduce","title":"3. Aggregation (Reduce)","text":"<p>Once all agents complete their work, the Aggregator Node synthesizes the results. It can perform cross-database operations (like calculating the difference between the two sub-queries) and formats the final answer for the user.</p>"},{"location":"architecture/routing/","title":"Routing Strategy","text":"<p>The platform operates in a specific \"Map-Reduce\" pattern to handle multi-datasource environments.</p>"},{"location":"architecture/routing/#the-challenge","title":"The Challenge","text":"<p>A user might ask: \"Compare the sales in our US Store (Postgres) vs. the EU Store (MySQL).\"</p> <p>This request cannot be solved by a single database query. It requires:</p> <ol> <li>Identifying that two different databases are involved.</li> <li>Splitting the question into two independent sub-queries.</li> <li>Routing each sub-query to the correct engine.</li> </ol>"},{"location":"architecture/routing/#the-solution-decomposer-node","title":"The Solution: Decomposer Node","text":"<p>The Decomposer Node is responsible for this logic.</p> <pre><code>graph LR\n    UserQuery --&gt; VectorSearch[\"Vector Search (Schema Index)\"]\n    VectorSearch --&gt; Context[\"Relevant Tables Context\"]\n    Context --&gt; Decomposer[\"Decomposer LLM\"]\n    Decomposer --&gt; SubQueryA[\"Query A -&gt; DB 1\"]\n    Decomposer --&gt; SubQueryB[\"Query B -&gt; DB 2\"]</code></pre>"},{"location":"architecture/routing/#1-vector-search-indexing","title":"1. Vector Search (Indexing)","text":"<p>Before runtime, we \"Index\" all datasources. This creates embeddings for:</p> <ul> <li>Table Names</li> <li>Column Descriptions</li> <li>Sample Data (Few-Shot examples)</li> </ul> <p>When a query arrives, we first search this index to find which tables are semantically relevant.</p>"},{"location":"architecture/routing/#2-decomposition","title":"2. Decomposition","text":"<p>The LLM is presented with the user question and the list of potentially relevant tables. It is asked to:</p> <ol> <li>Determine if the query is ambiguous or multi-part.</li> <li>If so, split it.</li> <li>Assign each split query to a specific <code>datasource_id</code>.</li> </ol>"},{"location":"architecture/routing/#3-fan-out-map","title":"3. Fan-Out (Map)","text":"<p>The system then spawns parallel execution branches. Each branch receives:</p> <ul> <li>The simplified sub-query.</li> <li>The specific <code>datasource_id</code>.</li> <li>The schema context for that datasource.</li> </ul>"},{"location":"architecture/security/","title":"Security Architecture","text":"<p>The NL2SQL Platform implements a multi-layered security approach designed to ensure that users can only access data they are explicitly authorized to see. This document outlines the security mechanisms for authentication, authorization, and query validation.</p>"},{"location":"architecture/security/#1-authentication-context","title":"1. Authentication &amp; Context","text":"<p>The platform assumes that the caller (API or CLI) has already authenticated the user. The user's identity and roles are passed into the execution pipeline via the <code>user_context</code> dictionary in the <code>GraphState</code>.</p>"},{"location":"architecture/security/#user-context-structure","title":"User Context Structure","text":"<p>The <code>user_context</code> must contain the following keys to enable authorization checks:</p> <pre><code>{\n  \"role\": \"sales_analyst\",\n  \"allowed_datasources\": [\"manufacturing_history\", \"manufacturing_supply\"],\n  \"allowed_tables\": [\n      \"customers\", \"sales_orders\", \"products\", \"inventory\"\n  ]\n}\n</code></pre> <ul> <li>role: A label for logging and auditing purposes.</li> <li>allowed_datasources: A list of <code>datasource_id</code> strings that the user can query. Use <code>[\"*\"]</code> for full access (e.g., Admin).</li> <li>allowed_tables: A list of table names the user can access. This enables fine-grained Row/Table-Level Security (RLS/TLS).</li> </ul>"},{"location":"architecture/security/#2-authorization-layers","title":"2. Authorization Layers","text":"<p>Security checks are performed at two distinct stages of the pipeline to fail fast and prevent unauthorized data access.</p>"},{"location":"architecture/security/#layer-1-datasource-access-routing","title":"Layer 1: Datasource Access (Routing)","text":"<p>Component: <code>DecomposerNode</code> &amp; <code>OrchestratorVectorStore</code></p> <p>Before any query planning begins, the system enforces Knowledge Isolation.</p> <ul> <li>Logic:<ol> <li>Extracts <code>allowed_datasources</code> from <code>state.user_context</code>.</li> <li>If the list is empty, the request is immediately rejected.</li> <li>Vector Store Partitioning: During context retrieval (RAG), the search is strictly filtered using <code>filter={\"datasource_id\": {\"$in\": allowed_ids}}</code>.</li> <li>This ensures that the LLM is never exposed to schema definitions or example questions from unauthorized datasources, effectively partitioning the \"knowledge base\" per user.</li> </ol> </li> </ul>"},{"location":"architecture/security/#layer-2-table-access-logical-validation","title":"Layer 2: Table Access (Logical Validation)","text":"<p>Component: <code>LogicalValidatorNode</code></p> <p>After the <code>PlannerNode</code> generates an abstract syntax tree (AST) for the query, the <code>LogicalValidatorNode</code> performs a strict policy check.</p> <ul> <li>Logic:<ol> <li>Extracts distinct table names from the <code>PlanModel</code> (AST).</li> <li>Compares them against <code>state.user_context[\"allowed_tables\"]</code>.</li> <li>If any table in the plan is not in the allowed list, a critical <code>SECURITY_VIOLATION</code> error is raised.</li> <li>The pipeline terminates immediately; the query is never generated or executed.</li> </ol> </li> </ul>"},{"location":"architecture/security/#3-query-safety-validation","title":"3. Query Safety &amp; Validation","text":"<p>Beyond RBAC, the system enforces strict structural constraints to prevent SQL injection and accidental mutation.</p>"},{"location":"architecture/security/#read-only-enforcement","title":"Read-Only Enforcement","text":"<p>The <code>LogicalValidatorNode</code> enforces that all generated plans are strictly <code>READ</code> operations (SELECT).</p> <ul> <li>Mutation statements (INSERT, UPDATE, DELETE, DROP) are structurally impossible to represent in the <code>PlanModel</code> AST.</li> <li>Even if the planner were to hallucinate a non-read type, the validator acts as a firewall and rejects it.</li> </ul>"},{"location":"architecture/security/#validated-ast-vs-raw-sql","title":"Validated AST vs. Raw SQL","text":"<p>The system does not rely on the LLM to generate raw SQL strings directly.</p> <ol> <li>Planner: Generates a typed JSON AST (<code>PlanModel</code>).</li> <li>Validator: Validates the AST logic and security.</li> <li>Generator: Compiles the validated AST into SQL using a deterministic compiler (<code>Visitor</code> pattern).</li> </ol> <p>This separation ensures that \"Prompt Injection\" attacks cannot easily force the model to output malicious SQL syntax, as the Generator controls the final output syntax.</p>"},{"location":"architecture/security/#4-resource-protection-dos","title":"4. Resource Protection (DoS)","text":"<p>To prevent Denial of Service (DoS) attacks or run-away queries, the system implements resource safeguards in the <code>ExecutorNode</code>.</p> <ul> <li>Cost Estimation Safeguard: Before execution, the system requests a cost estimate from the database adapter. If the estimated rows exceed <code>row_limit</code> (configured in <code>datasources.yaml</code>, default: 1000), execution is aborted with a <code>SAFEGUARD_VIOLATION</code>.</li> <li>Timeouts: Datasource configurations (<code>datasources.yaml</code>) support <code>statement_timeout_ms</code> to kill long-running queries at the driver level (Native enforcement for Postgres/MySQL).</li> <li>Payload Size Limit: The system enforces a strict memory limit (<code>max_bytes</code>) on the serialized result set. If the data returned by the adapter exceeds this limit (default: 10MB), the execution is halted to prevent OOM (Out Of Memory) crashes.</li> </ul>"},{"location":"architecture/security/#5-secret-management","title":"5. Secret Management","text":"<p>The platform employs a Pluggable Secret Management system (<code>nl2sql.secrets</code>) to handle sensitive credentials securely.</p>"},{"location":"architecture/security/#mechanism","title":"Mechanism","text":"<ul> <li>Configuration: Secrets providers are defined in <code>secrets.yaml</code>.</li> <li>Template Hydration: Secrets are referenced in other config files (like <code>datasources.yaml</code>) using the syntax <code>${provider_id:key}</code>.</li> <li>Resolution: The <code>SecretManager</code> resolves these references before the configuration is parsed, ensuring that sensitive values are never hardcoded in YAML files or committed to version control.</li> </ul>"},{"location":"architecture/security/#providers","title":"Providers","text":"<p>The system supports extensible providers via the <code>SecretProvider</code> protocol. You configure them in <code>secrets.yaml</code> with a unique <code>id</code>.</p> <ol> <li>Environment (<code>env</code>): Standard lookup (e.g., <code>${env:DB_PASS}</code>). Always available.</li> <li>AWS Secrets Manager: Defined by type <code>aws</code>. (e.g., <code>${aws-prod:db/pass}</code>).</li> <li>Azure Key Vault: Defined by type <code>azure</code>. (e.g., <code>${azure-main:db-secret}</code>).</li> <li>HashiCorp Vault: Defined by type <code>hashi</code>. (e.g., <code>${vault-internal:secret/data/db:pass}</code>).</li> </ol> <p>Dependencies: Cloud providers require optional extras (<code>nl2sql-core[aws]</code>, <code>nl2sql-core[azure]</code>, etc.) to keep the core lightweight.</p>"},{"location":"architecture/security/#6-configuration-security","title":"6. Configuration Security","text":""},{"location":"architecture/security/#strict-validation-v3","title":"Strict Validation (V3)","text":"<p>Datasource configurations are validated strictly at load time. This ensures:</p> <ul> <li>Type Safety: Malformed integers or booleans are rejected.</li> <li>Field Constraints: Unknown fields are forbidden, preventing \"config injection\" or typos.</li> <li>Sanitization: Passwords and sensitive fields are masked in logs.</li> <li>Adapter Specifics: Each adapter (e.g., <code>PostgresAdapter</code>) defines and validates its own configuration schema requirements.</li> </ul>"},{"location":"architecture/security/#61-policy-definition-configspoliciesjson","title":"6.1 Policy Definition (<code>configs/policies.json</code>)","text":"<p>The application uses Role-Based Access Control (RBAC). The <code>policies.json</code> file defines policies keyed by Role ID (e.g., <code>admin</code>, <code>analyst</code>).</p> <p>Strict Namespacing Rule: To prevent namespace collisions, <code>allowed_tables</code> MUST use the format <code>datasource_id.table_name</code>. Simple table names are not supported.</p>"},{"location":"architecture/security/#example","title":"Example","text":"<pre><code>{\n  \"sales_analyst\": {\n    \"description\": \"Access to Sales DB only\",\n    \"role\": \"analyst\",\n    \"allowed_datasources\": [\"sales_db\"],\n    \"allowed_tables\": [\n      // Exact Match\n      \"sales_db.orders\",\n\n      // Datasource Wildcard\n      \"sales_db.customers_*\"\n    ]\n  },\n  \"admin\": {\n    \"description\": \"Super Admin\",\n    \"role\": \"admin\",\n    \"allowed_tables\": [\"*\"]\n  }\n}\n</code></pre> <p>In CLI execution: <code>nl2sql run ... --role sales_analyst</code>. The application assumes the identity provider has already authenticated the user and assigned this role.</p>"},{"location":"architecture/security/#62-policy-schema-validation","title":"6.2 Policy Schema &amp; Validation","text":"<p>Policies are treated as Configuration Code. To prevent misconfiguration (e.g., typos, invalid types), the system validates <code>policies.json</code> against a strict Pydantic Schema at startup.</p> <p>Schema (<code>nl2sql.security.policies</code>):</p> <ol> <li>Strict Typing: Fields like <code>allowed_datasources</code> MUST be lists of strings.</li> <li>Syntax Enforcement: <code>allowed_tables</code> values are validated ensuring they match the <code>datasource_id.table_name</code> or wildcard format.</li> <li>Fail Fast: If the configuration is invalid, the application refuses to start, printing a clear error message describing the violation.</li> </ol>"},{"location":"architecture/security/#63-policy-management-cli","title":"6.3 Policy Management CLI","text":"<p>You can validate your policy file without running a query using the CLI.</p> <pre><code># Validate Syntax &amp; Integrity\nnl2sql policy validate\n</code></pre> <p>This command performs two checks:</p> <ol> <li>Schema Check: Validates syntax against the Pydantic model.</li> <li>Integrity Check: Verifies that referenced <code>datasources</code> and <code>tables</code> actually exist in <code>datasources.yaml</code>. Users often typo table names; this catches those errors before runtime.</li> </ol>"},{"location":"architecture/sql_agent/","title":"The SQL Agent","text":"<p>The SQL Agent is the core execution engine of the platform. For every relevant datasource identified by the routing layer, a dedicated SQL Agent is instantiated.</p> <p>Unlike traditional text-to-SQL systems that attempt to generate SQL in one shot, the SQL Agent treats the problem as a multi-step reasoning process.</p>"},{"location":"architecture/sql_agent/#execution-pipeline","title":"Execution Pipeline","text":"<p>The pipeline is modeled as a state machine:</p> <pre><code>graph TD\n    Planner[\"Planner Node\"] --&gt; LogicalValidator{\"Logical Validator\"}\n\n    LogicalValidator -- \"Valid\" --&gt; Generator[\"Generator Node\"]\n    LogicalValidator -- \"Invalid\" --&gt; Refiner[\"Refiner Node\"]\n\n    Generator --&gt; PhysicalValidator{\"Physical Validator\"}\n\n    PhysicalValidator -- \"Valid\" --&gt; Executor[\"Executor Node\"]\n    PhysicalValidator -- \"Invalid\" --&gt; Refiner\n\n    Refiner -- \"Feedback\" --&gt; Planner</code></pre>"},{"location":"architecture/sql_agent/#1-planner-node","title":"1. Planner Node","text":"<ul> <li>Input: User Query, Schema Context (Tables).</li> <li>Role: Reasoning &amp; Planning.</li> <li>Output: A recursive Abstract Syntax Tree (AST), not SQL.</li> <li>Why?: Generating an AST allows us to validate structure and intent before committing to a specific SQL dialect syntax.</li> </ul>"},{"location":"architecture/sql_agent/#2-logical-validator-node","title":"2. Logical Validator Node","text":"<ul> <li>Input: <code>PlanModel</code> (AST).</li> <li>Checks:</li> <li>Structure: Are joins valid? Do column aliases exist?</li> <li>Authorization (RBAC): Does the user have permission to access the referenced tables?</li> <li>Outcome: If invalid, the error is sent to the Refiner.</li> </ul>"},{"location":"architecture/sql_agent/#3-generator-node","title":"3. Generator Node","text":"<ul> <li>Input: Validated <code>PlanModel</code>.</li> <li>Role: Compiler.</li> <li>Output: Dialect-specific SQL string (e.g., T-SQL, PostgreSQL).</li> <li>Mechanism: Uses the Visitor pattern to traverse the AST and emit SQL compatible with the specific adapter's capabilities.</li> </ul>"},{"location":"architecture/sql_agent/#4-physical-validator-node","title":"4. Physical Validator Node","text":"<ul> <li>Input: Generated SQL String.</li> <li>Checks:</li> <li>Semantic: Performs a \"Dry Run\" (if supported) to check for runtime errors (e.g., function mismatches).</li> <li>Performance: Estimates query cost/rows to prevent database overload.</li> <li>Outcome: If dry run fails or cost is too high, it loops back to Refiner.</li> </ul>"},{"location":"architecture/sql_agent/#5-executor-node","title":"5. Executor Node","text":"<ul> <li>Input: Validated SQL.</li> <li>Role: Execution.</li> <li>Output: Raw result set.</li> </ul>"},{"location":"architecture/sql_agent/#6-refiner-node","title":"6. Refiner Node","text":"<ul> <li>Input: Validation Errors + Failed Plan.</li> <li>Role: Error Recovery.</li> <li>Mechanism: Uses an LLM to analyze why the plan failed and provides specific instructions to the Planner for the next attempt.</li> </ul>"},{"location":"guides/adapters/","title":"Adapter Development Guide","text":"<p>The NL2SQL Platform uses a modular Adapter architecture to support any database engine.</p>"},{"location":"guides/adapters/#1-overview","title":"1. Overview","text":"<p>Adapters bridge the gap between our semantic engine and specific database drivers. They implement the <code>DatasourceAdapter</code> protocol.</p>"},{"location":"guides/adapters/#2-creating-a-new-adapter","title":"2. Creating a New Adapter","text":""},{"location":"guides/adapters/#step-1-subclass-base-adapter","title":"Step 1: Subclass Base Adapter","text":"<p>Inherit from <code>BaseSQLAlchemyAdapter</code> to get free connection handling, introspection, and dry-run support.</p> <pre><code>from nl2sql_sqlalchemy_adapter import BaseSQLAlchemyAdapter\n\nclass MyDbAdapter(BaseSQLAlchemyAdapter):\n\n    def connect(self) -&gt; None:\n        \"\"\"Override to handle specific timeout or connection logic.\"\"\"\n        super().connect() \n</code></pre>"},{"location":"guides/adapters/#step-2-implement-protocol-methods","title":"Step 2: Implement Protocol Methods","text":"<p>You must ensure these methods work for your dialect:</p> <ul> <li><code>fetch_schema()</code>: (Handled by Base via SQLAlchemy Inspector)</li> <li><code>estimate_cost(sql)</code>: Return query cost/rows.</li> <li><code>dry_run(sql)</code>: Verify SQL without committing.</li> <li><code>explain(sql)</code>: Return execution plan.</li> </ul>"},{"location":"guides/adapters/#step-3-deployment","title":"Step 3: Deployment","text":"<ol> <li>Package your adapter (e.g. <code>nl2sql-adapter-mydb</code>).</li> <li>Register it via Python Entry Points in <code>pyproject.toml</code>:</li> </ol> <pre><code>[project.entry-points.\"nl2sql.adapters\"]\nmydb = \"nl2sql_mydb.adapter:MyDbAdapter\"\n</code></pre>"},{"location":"guides/adapters/#3-handling-timeouts","title":"3. Handling Timeouts","text":"<p>Your adapter receives <code>statement_timeout_ms</code> (int, milliseconds) in <code>__init__</code>.</p> <ul> <li>Standard approach: Use <code>BaseSQLAlchemyAdapter</code> logic (maps to <code>execution_options={\"timeout\": s}</code>).</li> <li>Native approach: Override <code>connect()</code> and inject dialect-specific arguments (e.g., <code>connect_args={\"options\": \"-c statement_timeout=...\"}</code> for Postgres).</li> </ul>"},{"location":"guides/adapters/#4-testing","title":"4. Testing","text":"<p>Use the <code>packages/adapter-sdk/tests/conftest.py</code> fixtures to verify your adapter against a real database instance.</p>"},{"location":"guides/benchmarking/","title":"Evaluation &amp; Benchmarking","text":"<p>The platform includes a comprehensive benchmarking suite to measure accuracy, stability, and routing performance.</p>"},{"location":"guides/benchmarking/#running-benchmarks","title":"Running Benchmarks","text":"<p>Benchmarks are run via the CLI.</p> <pre><code># Run the full Golden Set\npython -m nl2sql.cli --benchmark --dataset tests/golden_dataset.yaml\n</code></pre>"},{"location":"guides/benchmarking/#metrics","title":"Metrics","text":"<ol> <li>Execution Accuracy: Measures if the returned data matches the \"Ground Truth\".<ul> <li>Method: Executes Generated SQL vs Expected SQL and compares row sets.</li> </ul> </li> <li>Semantic Accuracy: Used when data mismatches. Uses an LLM Judge to check if the intent is equivalent.</li> <li>Routing Accuracy: Did we pick the right database?</li> <li>Stability (Pass@K): Runs each query <code>K</code> times to ensure results are deterministic.</li> </ol>"},{"location":"guides/benchmarking/#benchmarking-options","title":"Benchmarking Options","text":"Flag Description <code>--routing-only</code> Skips SQL generation; tests Decomposer only. <code>--iterations N</code> Runs each queries N times (Pass@N stability). <code>--export-path</code> Saves results to generic JSON/CSV."},{"location":"guides/cli_reference/","title":"CLI Reference","text":"<p>The <code>nl2sql</code> Command Line Interface (CLI) is the primary way to interact with the platform.</p>"},{"location":"guides/cli_reference/#global-options","title":"Global Options","text":"<p>These flags apply to all commands and must be specified before the subcommand.</p> <ul> <li><code>--env</code>, <code>-e TEXT</code>: Environment name (e.g. <code>dev</code>, <code>demo</code>, <code>prod</code>). Isolates configurations and vector stores. Defaults to <code>default</code> (Production).</li> </ul> <p>Example:</p> <pre><code># Uses configs/datasources.yaml\nnl2sql run \"query\"\n\n# Uses configs/datasources.demo.yaml\nnl2sql --env demo run \"query\"\n</code></pre>"},{"location":"guides/cli_reference/#commands","title":"Commands","text":""},{"location":"guides/cli_reference/#setup","title":"<code>setup</code>","text":"<p>Interactive wizard to initialize the platform.</p> <pre><code>nl2sql setup [OPTIONS]\n</code></pre> <p>Options:</p> <ul> <li><code>--demo</code>: Quickstart Mode. Automatically generates a \"Manufacturing\" demo environment with 4 SQLite databases and sample questions.</li> <li><code>--docker</code>: Used with <code>--demo</code> to generate a <code>docker-compose.yml</code> for full-fidelity testing (Postgres/MySQL) instead of SQLite.</li> </ul>"},{"location":"guides/cli_reference/#example-try-it-now","title":"Example: Try it Now","text":"<pre><code>nl2sql setup --demo\n</code></pre>"},{"location":"guides/cli_reference/#index","title":"<code>index</code>","text":"<p>Indexes database schemas and examples into the vector store for retrieval.</p> <pre><code>nl2sql index [OPTIONS]\n</code></pre> <p>Features:</p> <ul> <li>Schema Indexing: Introspects tables, columns, foreign keys, and comments.</li> <li>Example Indexing: Indexes sample questions for few-shot routing.</li> <li>Granular Feedback: Displays a checklist of indexed items per datasource.</li> <li>Summary Table: Shows total tables, columns, and examples indexed.</li> </ul> <p>Options:</p> <ul> <li><code>--config PATH</code>: Path to datasource config.</li> <li><code>--vector-store PATH</code>: Path to vector store directory.</li> </ul> <p>Example:</p> <pre><code>nl2sql --env demo index\n</code></pre>"},{"location":"guides/cli_reference/#run","title":"<code>run</code>","text":"<p>Executes a natural language query against the configured datasources.</p> <pre><code>nl2sql run [QUERY] [OPTIONS]\n</code></pre> <p>Arguments:</p> <ul> <li><code>QUERY</code>: The natural language question (e.g. \"Show me active users\").</li> </ul> <p>Options:</p> <ul> <li><code>--role TEXT</code>: The RBAC role to assume (e.g. <code>admin</code>, <code>analyst</code>). Defaults to <code>admin</code>.</li> <li><code>--no-exec</code>: Plan and Validate only, do not execute SQL.</li> <li><code>--verbose</code>, <code>-v</code>: Show detailed reasoning traces and intermediate node outputs.</li> <li><code>--show-perf</code>: Display timing metrics.</li> </ul> <p>Example:</p> <pre><code>nl2sql run \"Who bought the Bolt M5?\" --role sales_analyst --verbose\n</code></pre>"},{"location":"guides/cli_reference/#policy","title":"<code>policy</code>","text":"<p>Manage RBAC policies.</p> <ul> <li><code>validate</code>: Validates syntax and integrity of <code>policies.json</code>.</li> </ul> <pre><code>nl2sql policy validate\n</code></pre>"},{"location":"guides/cli_reference/#doctor","title":"<code>doctor</code>","text":"<p>Diagnoses environment issues (Python version, missing adapters, connectivity).</p> <pre><code>nl2sql doctor\n</code></pre>"},{"location":"guides/cli_reference/#benchmark","title":"<code>benchmark</code>","text":"<p>Runs the evaluation suite against a \"Golden Dataset\".</p> <pre><code>nl2sql benchmark --dataset configs/benchmark.yaml\n</code></pre>"},{"location":"guides/configuration/","title":"Configuration Guide","text":"<p>The NL2SQL Platform uses a strict, type-safe configuration system (Schema V3) defined in <code>datasources.yaml</code>.</p>"},{"location":"guides/configuration/#1-quick-start","title":"1. Quick Start","text":"<p>Run the setup wizard to generate a valid configuration:</p> <pre><code>nl2sql setup\n</code></pre>"},{"location":"guides/configuration/#2-secrets-management","title":"2. Secrets Management","text":"<p>Sensitive credentials should never be stored in plaintext. We support strict variable expansion using the <code>${provider_id:key}</code> syntax, powered by <code>secrets.yaml</code>.</p>"},{"location":"guides/configuration/#21-configuration-secretsyaml","title":"2.1 Configuration (<code>secrets.yaml</code>)","text":"<p>Define your secret providers in a <code>secrets.yaml</code> file (or <code>configs/secrets.yaml</code>).</p> <pre><code>version: 1\nproviders:\n  - id: azure-main\n    type: azure\n    vault_url: \"https://my-vault.vault.azure.net/\"\n    # You can resolve credentials from ENV here (Two-Phase Loading)\n    client_secret: \"${env:AZURE_CLIENT_SECRET}\"\n\n  - id: aws-prod\n    type: aws\n    region_name: us-east-1\n</code></pre>"},{"location":"guides/configuration/#22-usage-datasourcesyaml","title":"2.2 Usage (<code>datasources.yaml</code>)","text":"<p>Reference the secrets using the <code>id</code> defined above.</p> <p>[!IMPORTANT]</p> <ul> <li>Strict Syntax: Format must be exactly <code>${provider_id:key}</code>.</li> <li>Provider ID: Matches the <code>id</code> field in <code>secrets.yaml</code>.</li> <li>Environment: <code>${env:VAR}</code> is always available without config.</li> </ul> <p>Example:</p> <pre><code>connection:\n  host: localhost\n  # Uses 'aws-prod' provider defined in secrets.yaml\n  password: ${aws-prod:db/password}\n  # Uses built-in env provider\n  user: ${env:DB_USER}\n</code></pre> <p>Example:</p> <pre><code>connection:\n  host: localhost\n  password: ${env:DB_PASSWORD}  # Valid\n  # invalid_host: my-db-${env:ID}.com  &lt;-- Partial interpolation is NOT supported\n</code></pre>"},{"location":"guides/configuration/#3-supported-databases","title":"3. Supported Databases","text":""},{"location":"guides/configuration/#postgresql","title":"PostgreSQL","text":"<pre><code>- id: my_postgres\n  connection: \n    type: postgres\n    host: localhost\n    port: 5432\n    user: admin\n    password: ${env:PG_PASS}\n    database: analytics\n    # Options: require, prefer, verify-full\n    ssl_mode: prefer\n</code></pre>"},{"location":"guides/configuration/#mysql","title":"MySQL","text":"<pre><code>- id: my_mysql\n  connection:\n    type: mysql\n    user: admin\n    password: ${env:MYSQL_PASS}\n    database: ecommerce\n    # Optional: Use Unix Socket for local connection\n    unix_socket: /var/run/mysqld/mysqld.sock\n</code></pre>"},{"location":"guides/configuration/#sql-server-mssql-azure","title":"SQL Server (MSSQL / Azure)","text":"<p>Supports Standard, Windows, and Azure Authentication.</p> <p>Standard:</p> <pre><code>- id: mssql_std\n  connection:\n    type: mssql\n    host: sql.example.com\n    user: sa\n    password: ${env:SA_PASS}\n    database: master\n</code></pre> <p>Azure Service Principal:</p> <pre><code>- id: azure_db\n  connection:\n    type: mssql\n    host: my-server.database.windows.net\n    database: core_db\n    authentication: azure_sp\n    # Service Principal Credentials\n    client_id: ${env:AZURE_CLIENT_ID}\n    client_secret: ${env:AZURE_CLIENT_SECRET}\n    tenant_id: ${env:AZURE_TENANT_ID}\n</code></pre>"},{"location":"guides/configuration/#sqlite","title":"SQLite","text":"<pre><code>- id: local_sqlite\n  connection:\n    type: sqlite\n    database: /abs/path/to/db.sqlite\n</code></pre> <p>[!NOTE] The <code>connection.type</code> field determines which Adapter loads the datasource. For standard SQL databases, this matches the SQLAlchemy Dialect (e.g., <code>postgres</code>, <code>mysql</code>, <code>mssql</code>, <code>sqlite</code>). Custom adapters may define their own types.</p>"},{"location":"guides/configuration/#4-safety-limits","title":"4. Safety Limits","text":"<p>To prevent \"Out of Memory\" (OOM) crashes and protect the LLM context window, we enforce strict limits on query results.</p> Field Default Description <code>row_limit</code> 1000 Max rows returned by a query. <code>max_bytes</code> 10MB Hard Limit on payload size. Calculated via efficient row sampling (avg of first 50 rows). Queries exceeding this will fail safely. <p>Example:</p> <pre><code>options:\n  row_limit: 500\n  max_bytes: 5242880 # 5MB limit\n</code></pre>"},{"location":"guides/configuration/#5-ide-support-vs-code","title":"5. IDE Support (VS Code)","text":"<p>Your <code>datasources.yaml</code> includes a header that enables Autocomplete and Validation in VS Code:</p> <pre><code># yaml-language-server: $schema=./datasources.schema.json\n</code></pre> <p>Do not remove this line. It ensures your configuration matches the strict Pydantic models used by the engine.</p>"},{"location":"guides/configuration/#6-environment-variables","title":"6. Environment Variables","text":"<p>You can configure the application using the following environment variables (defined in <code>.env</code> or system environment).</p> Variable Default Description <code>OPENAI_API_KEY</code> - Required for LLM and Embedding services. <code>DATASOURCE_CONFIG</code> <code>configs/datasources.yaml</code> Path to the datasource configuration file. <code>SECRETS_CONFIG</code> <code>configs/secrets.yaml</code> Path to the secrets configuration file. <code>LLM_CONFIG</code> <code>configs/llm.yaml</code> Path to the LLM model configuration file. <code>POLICIES_CONFIG</code> <code>configs/policies.json</code> Path to the RBAC policies file. <code>VECTOR_STORE</code> <code>./chroma_db</code> Path (directory) to persist the vector store. <code>EMBEDDING_MODEL</code> <code>text-embedding-3-small</code> OpenAI embedding model name. <code>BENCHMARK_CONFIG</code> <code>configs/benchmark_suite.yaml</code> Path to accurate testing suite configuration. <code>ROUTING_EXAMPLES</code> <code>configs/sample_questions.yaml</code> Path to examples used for few-shot routing. <code>ROUTER_L1_THRESHOLD</code> <code>0.4</code> Threshold for Vector Search relevance. <code>ROUTER_L2_THRESHOLD</code> <code>0.6</code> Threshold for Multi-Query voting agreement."},{"location":"guides/deployment/","title":"Deployment Guide","text":"<p>This guide covers deploying the NL2SQL platform in a production environment using Docker.</p>"},{"location":"guides/deployment/#architecture","title":"Architecture","text":"<p>In production, the system typically runs as a backend API service.</p> <pre><code>graph TD\n    Client[\"Web UI / Dashboard\"] --&gt; LoadBalancer\n    LoadBalancer --&gt; API[\"NL2SQL API (FastAPI)\"]\n    API --&gt; VectorDB[\"Vector Store (Chroma)\"]\n    API --&gt; Databases[\"Target Databases\"]</code></pre>"},{"location":"guides/deployment/#docker-compose","title":"Docker Compose","text":"<p>Here is an example <code>docker-compose.yml</code> for deploying the API service:</p> <pre><code>version: '3.8'\nservices:\n  api:\n    build: .\n    ports:\n      - \"8000:8000\"\n    environment:\n      - OPENAI_API_KEY=${OPENAI_API_KEY}\n      - CONFIG_PATH=/app/configs/datasources.yaml\n    volumes:\n      - ./configs:/app/configs\n</code></pre>"},{"location":"guides/deployment/#production-checklist","title":"Production Checklist","text":"<ol> <li>[ ] Security: Ensure <code>configs/policies.json</code> is mapped to your identity provider (e.g. OAuth) to dynamically enforce <code>allowed_tables</code>.</li> <li>[ ] Read-Only: Configure the database users in <code>datasources.yaml</code> to have READ-ONLY permissions at the database level.     &gt; The PhysicalValidator provides a safety net, but deep defense requires DB-level grants.</li> <li>[ ] Monitoring: Enable the <code>PipelineMonitorCallback</code> to log all traces to your observability stack (e.g. LangSmith).</li> </ol>"},{"location":"guides/development/","title":"Development Guide","text":"<p>How to contribute to the platform.</p>"},{"location":"guides/development/#setup","title":"Setup","text":"<pre><code># Windows PowerShell\n./scripts/setup_dev.ps1\n</code></pre>"},{"location":"guides/development/#running-tests","title":"Running Tests","text":"<p>We use <code>pytest</code>.</p> <pre><code># Run Unit Tests (Fast)\npython -m pytest packages/core/tests/unit\n\n# Run Integration Tests (Requires Docker)\ndocker compose up -d\npython -m pytest packages/core/tests/integration\n</code></pre>"},{"location":"guides/development/#adding-a-new-node","title":"Adding a New Node","text":"<ol> <li>Create the node class in <code>packages/core/src/nl2sql/pipeline/nodes/</code>.</li> <li>Implement <code>__call__(self, state: GraphState) -&gt; Dict</code>.</li> <li>Register it in <code>graph.py</code> or the relevant subgraph (<code>sql_agent.py</code>).</li> <li>Add unit tests.</li> </ol>"},{"location":"guides/getting_started/","title":"Getting Started","text":"<p>This guide will help you install the platform and run your first query.</p>"},{"location":"guides/getting_started/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.10+</li> <li>Docker (for running integrations)</li> </ul>"},{"location":"guides/getting_started/#1-installation","title":"1. Installation","text":"<p>The platform is designed as a monorepo. You should install the core and the adapters you need.</p> <pre><code># Clone the repository\ngit clone https://github.com/nadeem4/nl2sql.git\ncd nl2sql\n\n# Install Core\npip install -e packages/adapter-sdk\npip install -e packages/cli # Installs 'nl2sql' command\n\n# Install Adapters (e.g. Postgres)\npip install -e packages/adapters/postgres\n</code></pre>"},{"location":"guides/getting_started/#2-setup","title":"2. Setup","text":"<p>Run the interactive setup wizard. This will inspect your environment, guide you through creating a configuration, and index your database schema.</p> <pre><code>nl2sql setup\n</code></pre> <p>The wizard will ask for:</p> <ol> <li>Database Details (Host, Port, User, Password).</li> <li>LLM Provider (OpenAI, Gemini, Ollama).</li> <li>Confirmation to install required Adapters (e.g. <code>nl2sql-postgres</code>).</li> </ol>"},{"location":"guides/getting_started/#3-run-a-query","title":"3. Run a Query","text":"<p>Now you are ready to ask questions!</p> <pre><code>nl2sql run \"Show me the top 5 users by sales\"\n</code></pre> <p>The system will:</p> <ol> <li>Plan the query.</li> <li>Route it to the correct database.</li> <li>Generate and Validate SQL.</li> <li>Execute and display the results.</li> </ol>"},{"location":"guides/getting_started/#next-steps","title":"Next Steps","text":"<p>Explore the full capabilities of the CLI:</p> <ul> <li>CLI Reference - Learn about <code>--env</code>, <code>--role</code>, and <code>index</code> options.</li> <li>Configuration - Connect to your own databases.</li> </ul>"},{"location":"guides/production_roadmap/","title":"Production Roadmap","text":"<p>Steps to transition from MVP to Mission-Critical Service.</p>"},{"location":"guides/production_roadmap/#phase-1-security-governance","title":"Phase 1: Security &amp; Governance","text":"<ul> <li>[ ] Secret Management: Move credentials from <code>datasources.yaml</code> to AWS Secrets Manager / Azure Key Vault.</li> <li>[ ] Dependency Locking: Generate <code>poetry.lock</code> or <code>uv.lock</code>.</li> <li>[ ] SQL Governance: Implement a \"Policy Agent\" to block request types (e.g. <code>CROSS JOIN</code> on large tables).</li> </ul>"},{"location":"guides/production_roadmap/#phase-2-reliability","title":"Phase 2: Reliability","text":"<ul> <li>[ ] Circuit Breakers: Enforce strict timeouts (e.g. 5s) at the driver level.</li> <li>[ ] Rate Limiting: Implement per-tenant token budgeting.</li> </ul>"},{"location":"guides/production_roadmap/#phase-3-observability","title":"Phase 3: Observability","text":"<ul> <li>[ ] Audit Logging: Write immutable execution logs (User, Query, SQL) to compliance storage.</li> <li>[ ] Async Concurrency: Refactor Core to <code>ainvoke</code> for higher throughput.</li> </ul>"},{"location":"nodes/aggregator_node/","title":"AggregatorNode","text":""},{"location":"nodes/aggregator_node/#purpose","title":"Purpose","text":"<p>The <code>AggregatorNode</code> combines results from the execution phase and prepares the final response. It implements a \"Fast Path\" for direct data streaming and a \"Slow Path\" for LLM-based summarization or answer synthesis.</p>"},{"location":"nodes/aggregator_node/#class-reference","title":"Class Reference","text":"<ul> <li>Class: <code>AggregatorNode</code></li> <li>Path: <code>packages/core/src/nl2sql/pipeline/nodes/aggregator/node.py</code></li> </ul>"},{"location":"nodes/aggregator_node/#inputs","title":"Inputs","text":"<p>The node reads the following fields from <code>GraphState</code>:</p> <ul> <li><code>state.user_query</code> (str): The user's question.</li> <li><code>state.intermediate_results</code> (List): Results from the executor(s).</li> <li><code>state.output_mode</code> (str): \"data\" (Fast Path) or \"summary\"/\"verbose\" (Slow Path).</li> <li><code>state.errors</code> (List[PipelineError]): Any errors to include in the summary.</li> </ul>"},{"location":"nodes/aggregator_node/#outputs","title":"Outputs","text":"<p>The node updates the following fields in <code>GraphState</code>:</p> <ul> <li><code>state.final_answer</code> (Any): The final text entry or data payload.</li> <li><code>state.reasoning</code> (List[Dict]): Log of which path was taken.</li> </ul>"},{"location":"nodes/aggregator_node/#logic-flow","title":"Logic Flow","text":"<ol> <li>Fast Path Check:<ul> <li>If there is exactly one result, no errors, and <code>output_mode</code> is \"data\":</li> <li>Returns the raw data directly.</li> </ul> </li> <li>Slow Path (LLM Aggregation):<ul> <li>Formats all <code>intermediate_results</code> (and errors) into a string.</li> <li>Prompts the LLM to synthesize an answer to the <code>user_query</code> using the provided data.</li> <li>Formats the LLM output (Table/List/Text).</li> <li>Returns the generated summary.</li> </ul> </li> </ol>"},{"location":"nodes/aggregator_node/#error-handling","title":"Error Handling","text":"<ul> <li><code>AGGREGATOR_FAILED</code>: If the LLM summarization fails.</li> </ul>"},{"location":"nodes/decomposer_node/","title":"DecomposerNode","text":""},{"location":"nodes/decomposer_node/#purpose","title":"Purpose","text":"<p>The <code>DecomposerNode</code> acts as the entry point and router for the pipeline. It is responsible for analyzing the user's query to determine which datasource(s) should handle the request. For complex requests, it can break the query down into sub-queries (though simple routing is the primary function). It also checks user authorization before proceeding.</p>"},{"location":"nodes/decomposer_node/#class-reference","title":"Class Reference","text":"<ul> <li>Class: <code>DecomposerNode</code></li> <li>Path: <code>packages/core/src/nl2sql/pipeline/nodes/decomposer/node.py</code></li> </ul>"},{"location":"nodes/decomposer_node/#inputs","title":"Inputs","text":"<p>The node reads the following fields from <code>GraphState</code>:</p> <ul> <li><code>state.user_query</code> (str): The initial user question.</li> <li><code>state.user_context</code> (Dict): User session data, specifically <code>allowed_datasources</code> for authorization.</li> <li><code>state.semantic_analysis</code> (SemanticAnalysisResponse): Used to expand the query with keywords/synonyms for better vector retrieval.</li> </ul>"},{"location":"nodes/decomposer_node/#outputs","title":"Outputs","text":"<p>The node updates the following fields in <code>GraphState</code>:</p> <ul> <li><code>state.sub_queries</code> (List[SubQuery]): A list of routed queries. Each <code>SubQuery</code> contains:</li> <li><code>question</code>: The specific question for the datasource.</li> <li><code>datasource_id</code>: The ID of the chosen datasource.</li> <li><code>state.confidence</code> (float): The confidence score of the routing decision.</li> <li><code>state.reasoning</code> (List[Dict]): Explanation of why a specific datasource was selected.</li> <li><code>state.errors</code> (List[PipelineError]): <code>SECURITY_VIOLATION</code> if the user lacks access.</li> </ul>"},{"location":"nodes/decomposer_node/#logic-flow","title":"Logic Flow","text":"<ol> <li>Authorization Check: Verifies if <code>state.user_context</code> contains accessible datasources. If not, returns <code>SECURITY_VIOLATION</code>.</li> <li>Query Expansion: If <code>state.semantic_analysis</code> is present, it augments the query with keywords and synonyms to improve retrieval recall.</li> <li>Context Retrieval:<ul> <li>Queries the <code>OrchestratorVectorStore</code> using the expanded query.</li> <li>Retrieves relevant table schemas and datasource descriptions.</li> </ul> </li> <li>LLM Routing:<ul> <li>Uses the LLM to analyze the retrieved context and the user query.</li> <li>Decides which datasource is best suited to answer the question.</li> </ul> </li> <li>Output Generation: Returns the routing decision (datasource selection) and confidence score.</li> </ol>"},{"location":"nodes/decomposer_node/#error-handling","title":"Error Handling","text":"<ul> <li><code>SECURITY_VIOLATION</code>: Critical error if the user has no allowed datasources.</li> <li>Retrieval Warnings: Logs warnings if no relevant documents are found in the vector store.</li> </ul>"},{"location":"nodes/executor_node/","title":"ExecutorNode","text":""},{"location":"nodes/executor_node/#purpose","title":"Purpose","text":"<p>The <code>ExecutorNode</code> is responsible for executing the generated SQL query against the target datasource. It handles connection management via the <code>DatasourceRegistry</code> adapters, safeguards against massive result sets, and formats the output.</p>"},{"location":"nodes/executor_node/#class-reference","title":"Class Reference","text":"<ul> <li>Class: <code>ExecutorNode</code></li> <li>Path: <code>packages/core/src/nl2sql/pipeline/nodes/executor/node.py</code></li> </ul>"},{"location":"nodes/executor_node/#inputs","title":"Inputs","text":"<p>The node reads the following fields from <code>GraphState</code>:</p> <ul> <li><code>state.sql_draft</code> (str): The SQL query to execute.</li> <li><code>state.selected_datasource_id</code> (str): The target database ID.</li> </ul>"},{"location":"nodes/executor_node/#outputs","title":"Outputs","text":"<p>The node updates the following fields in <code>GraphState</code>:</p> <ul> <li><code>state.execution</code> (<code>ExecutionModel</code>): The result of the query.</li> <li><code>columns</code> (List[str]): Column names.</li> <li><code>rows</code> (List[Dict]): The data returned.</li> <li><code>row_count</code> (int): Number of rows.</li> <li><code>state.errors</code> (List[PipelineError]): Errors during execution.</li> </ul>"},{"location":"nodes/executor_node/#logic-flow","title":"Logic Flow","text":"<ol> <li>Validation: Ensures <code>sql_draft</code> and <code>datasource_id</code> are present.</li> <li>Adapter Retrieval: Fetches the correct adapter (e.g., PostgresAdapter) from the registry.</li> <li>Cost Estimation (Safeguard):<ul> <li>If supported by the adapter, estimates the query cost.</li> <li>If the estimated row count exceeds <code>SAFEGUARD_ROW_LIMIT</code> (10,000), aborts execution and raises <code>SAFEGUARD_VIOLATION</code>.</li> </ul> </li> <li>Execution:<ul> <li>Runs <code>adapter.execute(sql)</code>.</li> <li>Captures the result set.</li> </ul> </li> <li>Formatting: Converts the results into the standard <code>ExecutionModel</code>.</li> </ol>"},{"location":"nodes/executor_node/#error-handling","title":"Error Handling","text":"<ul> <li><code>SAFEGUARD_VIOLATION</code>: If the query is predicted to return too many rows.</li> <li><code>DB_EXECUTION_ERROR</code>: If the database raises an exception (e.g., timeout, syntax error not caught by validator).</li> </ul>"},{"location":"nodes/generator_node/","title":"GeneratorNode","text":""},{"location":"nodes/generator_node/#purpose","title":"Purpose","text":"<p>The <code>GeneratorNode</code> is the compiler of the pipeline. It takes the abstract execution plan (<code>PlanModel</code>) produced by the Planner and generates a valid, dialect-specific SQL string. It uses <code>sqlglot</code> to transpile the internal AST into the target SQL dialect (e.g., PostgreSQL, T-SQL, MySQL), enforcing syntactic correctness.</p>"},{"location":"nodes/generator_node/#class-reference","title":"Class Reference","text":"<ul> <li>Class: <code>GeneratorNode</code></li> <li>Path: <code>packages/core/src/nl2sql/pipeline/nodes/generator/node.py</code></li> </ul>"},{"location":"nodes/generator_node/#inputs","title":"Inputs","text":"<p>The node reads the following fields from <code>GraphState</code>:</p> <ul> <li><code>state.plan</code> (<code>PlanModel</code>): The logical plan to compile.</li> <li><code>state.selected_datasource_id</code> (str): The ID of the target database, used to determine the SQL dialect.</li> </ul>"},{"location":"nodes/generator_node/#outputs","title":"Outputs","text":"<p>The node updates the following fields in <code>GraphState</code>:</p> <ul> <li><code>state.sql_draft</code> (str): The generated SQL query string.</li> <li><code>state.reasoning</code> (List[Dict]): Logs the generated SQL.</li> <li><code>state.errors</code> (List[PipelineError]): <code>SQL_GEN_FAILED</code> if compilation errors occur.</li> </ul>"},{"location":"nodes/generator_node/#logic-flow","title":"Logic Flow","text":"<ol> <li>Validation: Checks if a plan and a datasource ID are present in the state.</li> <li>Profile Lookup: Fetches the <code>dialect</code> (e.g., \"postgres\", \"tsql\") and default <code>row_limit</code> from the datasource registry.</li> <li>AST Transformation (<code>SqlVisitor</code>):<ul> <li>The node uses a <code>SqlVisitor</code> class to traverse the <code>PlanModel</code> (Expr tree).</li> <li>It builds a corresponding <code>sqlglot</code> Expression tree.</li> <li>This visitor handles literals, columns, functions, binary/unary operations, and case statements.</li> </ul> </li> <li>SQL Synthesis:<ul> <li>Constructs the top-level <code>SELECT</code> statement using <code>sqlglot</code> builders.</li> <li>Applies transformations for <code>SELECT</code>, <code>FROM</code> (Tables), <code>JOIN</code>, <code>WHERE</code>, <code>GROUP BY</code>, <code>HAVING</code>, <code>ORDER BY</code>, and <code>LIMIT</code>.</li> <li>Handles dialect-specific nuances (e.g., quoting identifiers, function names) via <code>sqlglot.transpile</code> mechanisms (implicit in <code>.sql(dialect=...)</code>).</li> </ul> </li> <li>Output: Returns the final SQL string.</li> </ol>"},{"location":"nodes/generator_node/#error-handling","title":"Error Handling","text":"<ul> <li><code>SQL_GEN_FAILED</code>: Raised if the visitor encounters unknown expression types or if <code>sqlglot</code> fails to generate the string.</li> </ul>"},{"location":"nodes/planner_node/","title":"PlannerNode","text":""},{"location":"nodes/planner_node/#purpose","title":"Purpose","text":"<p>The <code>PlannerNode</code> is the cognitive core of the SQL generation process. It synthesizes the user's intent and the retrieved schema (tables, columns) to create a structured, dialect-agnostic \"Execution Plan\" (<code>PlanModel</code>). Unlike a simple text-to-SQL prompt, this node generates a deterministic Abstract Syntax Tree (AST), ensuring strict adherence to the schema and enabling logical validation before any SQL is written.</p>"},{"location":"nodes/planner_node/#class-reference","title":"Class Reference","text":"<ul> <li>Class: <code>PlannerNode</code></li> <li>Path: <code>packages/core/src/nl2sql/pipeline/nodes/planner/node.py</code></li> </ul>"},{"location":"nodes/planner_node/#inputs","title":"Inputs","text":"<p>The node reads the following fields from <code>GraphState</code>:</p> <ul> <li><code>state.user_query</code> (str): The original natural language question.</li> <li><code>state.relevant_tables</code> (List[Table]): The list of schema definitions (tables/columns) found relevant by the Decomposer.</li> <li><code>state.semantic_analysis</code> (SemanticAnalysisResponse): Enriched context (keywords, synonyms) to guide planning.</li> <li><code>state.errors</code> (List[PipelineError]): Existing errors (if in a re-planning loop), used to provide feedback to the LLM.</li> <li><code>state.selected_datasource_id</code> (str): ID of the target database, used to fetch dialect-specific settings (e.g., date formats).</li> </ul>"},{"location":"nodes/planner_node/#outputs","title":"Outputs","text":"<p>The node updates the following fields in <code>GraphState</code>:</p> <ul> <li><code>state.plan</code> (<code>PlanModel</code>): The comprehensive execution plan containing:</li> <li>tables: List of <code>TableNode</code> (name, alias, schema).</li> <li>joins: List of <code>JoinNode</code> (right table, condition, join type).</li> <li>select_items: List of <code>SelectItemNode</code> (expression, alias).</li> <li>where: Root <code>Expr</code> node for filtering.</li> <li>group_by: List of <code>GroupByNode</code>.</li> <li>having: Root <code>Expr</code> node for post-aggregation filtering.</li> <li>order_by: List of <code>OrderByNode</code>.</li> <li>limit: Integer row limit.</li> <li><code>state.reasoning</code> (List[Dict]): Log entry explaining the planning decisions.</li> <li><code>state.errors</code> (List[PipelineError]): Appends <code>MISSING_LLM</code> or <code>PLANNING_FAILURE</code> if generation fails.</li> </ul>"},{"location":"nodes/planner_node/#logic-flow","title":"Logic Flow","text":"<ol> <li>Initialization: Checks if the LLM is provided; otherwise returns a critical error.</li> <li>Context Assembly:<ul> <li>Serializes <code>state.relevant_tables</code> into a schema string.</li> <li>Formats previous <code>state.errors</code> into a feedback string (for self-correction).</li> <li>Retrieves the <code>date_format</code> from the selected datasource profile.</li> <li>includes <code>state.semantic_analysis</code> context.</li> </ul> </li> <li>LLM Invocation:<ul> <li>Prompts the LLM with the query, schema, examples, and feedback.</li> <li>Expects a JSON response conforming to <code>PlanModel</code>.</li> </ul> </li> <li>Post-Processing:<ul> <li>Validates and parses the JSON into the <code>PlanModel</code> Pydantic object.</li> <li>Updates <code>state.plan</code> and logs reasoning.</li> </ul> </li> </ol>"},{"location":"nodes/planner_node/#error-handling","title":"Error Handling","text":"<ul> <li><code>MISSING_LLM</code>: Raised if the node is initialized without a language model.</li> <li><code>PLANNING_FAILURE</code>: Raised if the LLM output is malformed or cannot be parsed into <code>PlanModel</code>.</li> </ul>"},{"location":"nodes/refiner_node/","title":"RefinerNode","text":""},{"location":"nodes/refiner_node/#purpose","title":"Purpose","text":"<p>The <code>RefinerNode</code> operates in the self-correction loop. When validation or execution fails, the Refiner analyzes the error, the failed plan, and the schema to generate constructive feedback (Natural Language Advice) for the Planner, enabling it to retry and fix the mistake.</p>"},{"location":"nodes/refiner_node/#class-reference","title":"Class Reference","text":"<ul> <li>Class: <code>RefinerNode</code></li> <li>Path: <code>packages/core/src/nl2sql/pipeline/nodes/refiner/node.py</code></li> </ul>"},{"location":"nodes/refiner_node/#inputs","title":"Inputs","text":"<p>The node reads the following fields from <code>GraphState</code>:</p> <ul> <li><code>state.user_query</code> (str): The original intent.</li> <li><code>state.plan</code> (<code>PlanModel</code>): The plan that failed.</li> <li><code>state.errors</code> (List[PipelineError]): The specific errors (e.g., \"Table not found\", \"Execution error\").</li> <li><code>state.relevant_tables</code>: The schema context.</li> </ul>"},{"location":"nodes/refiner_node/#outputs","title":"Outputs","text":"<p>The node updates the following fields in <code>GraphState</code>:</p> <ul> <li><code>state.errors</code> (List[PipelineError]): Appends a new error of type <code>PLAN_FEEDBACK</code> containing the LLM's advice.</li> <li><code>state.reasoning</code> (List[Dict]): The feedback generated.</li> </ul>"},{"location":"nodes/refiner_node/#logic-flow","title":"Logic Flow","text":"<ol> <li>Context Assembly:<ul> <li>Dumps the <code>relevant_tables</code>, <code>failed_plan</code>, and <code>errors</code> into strings.</li> </ul> </li> <li>LLM Analysis:<ul> <li>Prompts the LLM to diagnose the failure.</li> <li>Asks: \"Given this query, this plan, and these errors, what went wrong and how should the planner fix it?\"</li> </ul> </li> <li>Feedback Injection:<ul> <li>Wraps the LLM's response in a <code>PipelineError</code> (Severity WARNING).</li> <li>This error is read by the <code>PlannerNode</code> in the next iteration as \"Feedback\".</li> </ul> </li> </ol>"},{"location":"nodes/refiner_node/#error-handling","title":"Error Handling","text":"<ul> <li><code>REFINER_FAILED</code>: If the refinement LLM call fails.</li> </ul>"},{"location":"nodes/semantic_analysis_node/","title":"SemanticAnalysisNode","text":""},{"location":"nodes/semantic_analysis_node/#purpose","title":"Purpose","text":"<p>The <code>SemanticAnalysisNode</code> is a preprocessing step that normalizes the user query and extracts metadata such as keywords and synonyms. This enriched context supports both the <code>DecomposerNode</code> (by expanding the search query for better retrieval) and the <code>PlannerNode</code> (by resolving ambiguity).</p>"},{"location":"nodes/semantic_analysis_node/#class-reference","title":"Class Reference","text":"<ul> <li>Class: <code>SemanticAnalysisNode</code></li> <li>Path: <code>packages/core/src/nl2sql/pipeline/nodes/semantic/node.py</code></li> </ul>"},{"location":"nodes/semantic_analysis_node/#inputs","title":"Inputs","text":"<p>The node reads the following fields from <code>GraphState</code>:</p> <ul> <li><code>state.user_query</code> (str): The raw user input.</li> </ul>"},{"location":"nodes/semantic_analysis_node/#outputs","title":"Outputs","text":"<p>The node updates the following fields in <code>GraphState</code>:</p> <ul> <li><code>state.semantic_analysis</code> (<code>SemanticAnalysisResponse</code>):</li> <li><code>canonical_query</code>: Normalized form of the question.</li> <li><code>keywords</code>: Extracted domain keywords.</li> <li><code>synonyms</code>: List of potential synonyms for columns/tables.</li> <li><code>reasoning</code>: The analysis thought process.</li> </ul>"},{"location":"nodes/semantic_analysis_node/#logic-flow","title":"Logic Flow","text":"<ol> <li>LLM Invocation:<ul> <li>Prompts the LLM with the <code>user_query</code>.</li> <li>Requests an analysis including canonicalization and keyword extraction.</li> </ul> </li> <li>Result Storage:<ul> <li>Stores the structured response in <code>state.semantic_analysis</code>.</li> <li>Logs the reasoning and keywords.</li> </ul> </li> </ol>"},{"location":"nodes/semantic_analysis_node/#error-handling","title":"Error Handling","text":"<ul> <li>Fallback: If the LLM call fails, it defaults to returning the raw query with empty keywords to prevent pipeline blockage.</li> </ul>"},{"location":"nodes/validator_node/","title":"LogicalValidatorNode","text":""},{"location":"nodes/validator_node/#purpose","title":"Purpose","text":"<p>The <code>LogicalValidatorNode</code> validates the generated AST (<code>PlanModel</code>) before any SQL is generated. It performs static analysis to ensure the plan structure is valid (e.g., no duplicate aliases) and that all referenced tables and columns actually exist in the schema. It also enforces access control policies.</p>"},{"location":"nodes/validator_node/#class-reference","title":"Class Reference","text":"<ul> <li>Class: <code>LogicalValidatorNode</code></li> <li>Path: <code>packages/core/src/nl2sql/pipeline/nodes/validator/node.py</code></li> </ul>"},{"location":"nodes/validator_node/#inputs","title":"Inputs","text":"<p>The node reads the following fields from <code>GraphState</code>:</p> <ul> <li><code>state.plan</code> (<code>PlanModel</code>): The plan to validate.</li> <li><code>state.relevant_tables</code> (List[Table]): The schema context to check against.</li> <li><code>state.user_context</code> (Dict): Used for policy validation (<code>allowed_tables</code>).</li> </ul>"},{"location":"nodes/validator_node/#outputs","title":"Outputs","text":"<p>The node updates the following fields in <code>GraphState</code>:</p> <ul> <li><code>state.errors</code> (List[PipelineError]): Appends error validation failures found.</li> <li><code>TABLE_NOT_FOUND</code>: Referenced table does not exist in <code>relevant_tables</code>.</li> <li><code>INVALID_PLAN_STRUCTURE</code>: Malformed AST (e.g., non-contiguous ordinals).</li> <li><code>SECURITY_VIOLATION</code>: Reference to unauthorized table.</li> </ul>"},{"location":"nodes/validator_node/#logic-flow","title":"Logic Flow","text":"<ol> <li>Duplicate Alias Check: Ensures all table aliases in the plan are unique.</li> <li>Schema Verification (<code>_build_alias_map</code>):<ul> <li>Iterates through <code>plan.tables</code>.</li> <li>Verifies each table exists in <code>state.relevant_tables</code>.</li> <li>Maps aliases to valid columns for downstream checks.</li> </ul> </li> <li>Policy Validation:<ul> <li>Checks if the user is authorized to access the referenced tables based on <code>state.user_context</code>.</li> </ul> </li> <li>Static Validation:<ul> <li>(Implied) Additional checks on the structure of the AST.</li> </ul> </li> </ol>"},{"location":"nodes/validator_node/#error-handling","title":"Error Handling","text":"<ul> <li><code>TABLE_NOT_FOUND</code>: If the plan invents a table name.</li> <li><code>SECURITY_VIOLATION</code>: If a table is restricted by policy.</li> </ul>"},{"location":"reference/cli/","title":"CLI Reference","text":"<p>The NL2SQL command line interface uses a subcommand structure.</p> <p>Command: <code>nl2sql [COMMAND] [OPTIONS]</code></p>"},{"location":"reference/cli/#commands","title":"Commands","text":""},{"location":"reference/cli/#setup","title":"<code>setup</code>","text":"<p>Interactively configure the environment, create config files, and index schemas.</p> Option Description <code>None</code> Runs the interactive wizard."},{"location":"reference/cli/#run","title":"<code>run</code>","text":"<p>Execute natural language queries against your datasources.</p> <p>Usage: <code>nl2sql run \"Your query here\"</code></p> Option Description Default <code>--config</code> Path to datasources YAML. <code>configs/datasources.yaml</code> <code>--llm-config</code> Path to LLM configuration. <code>configs/llm.yaml</code> <code>--id</code> Target specific datasource ID (bypass routing). auto-route <code>--json</code> Output result as raw JSON only. <code>False</code> <code>--no-exec</code> Generate and Validate SQL only (skip execution). <code>False</code> <code>--user</code> Context user ID for RBAC checks. <code>admin</code>"},{"location":"reference/cli/#doctor","title":"<code>doctor</code>","text":"<p>Diagnose environment issues, check dependencies, and verify connectivity.</p> Option Description <code>None</code> Runs diagnositc checks."},{"location":"reference/cli/#index","title":"<code>index</code>","text":"<p>Manually trigger the schema indexing process.</p> Option Description Default <code>--config</code> Path to datasources YAML. <code>configs/datasources.yaml</code> <code>--llm-config</code> Path to LLM configuration. <code>configs/llm.yaml</code>"},{"location":"reference/cli/#list-adapters","title":"<code>list-adapters</code>","text":"<p>List all currently installed database adapter packages.</p>"},{"location":"reference/cli/#benchmark","title":"<code>benchmark</code>","text":"<p>Run the evaluation suite.</p> Option Description Default <code>--config</code> Path to benchmark suite YAML. <code>configs/benchmark.yaml</code> <code>--output</code> Directory to save report artifacts. <code>benchmarks/</code>"},{"location":"reference/lessons_learned/","title":"Lessons Learned","text":"<p>A collection of architectural insights/gotchas discovered during development.</p>"},{"location":"reference/lessons_learned/#1-context-retrieval-bias-signal-density","title":"1. Context Retrieval Bias (Signal Density)","text":"<p>Issue: The Decomposer exhibits a \"Table-First Bias\". It prioritizes datasources with matched Tables over those with matched Examples.</p> <p>Scenario:</p> <ul> <li>Query: \"Show production runs for 'Widget A'\"</li> <li><code>db_history</code>: Returns <code>production_runs</code> table. (Strong Signal)</li> <li><code>db_supply</code>: Returns NO tables, but matches example \"Who supplies Widget A?\". (Weak Signal)</li> </ul> <p>Result: The Decomposer ignores <code>db_supply</code> because the prompt prioritized schema.</p> <p>Fix: The <code>DECOMPOSER_PROMPT</code> was updated to treat Matched Examples as a valid routing signal, even if no tables are returned.</p>"},{"location":"reference/test_data/","title":"Database Setup &amp; Schema Documentation","text":"<p>This document outlines the database environment used in the NL2SQL pipeline, including datasources, schemas, and data volume.</p>"},{"location":"reference/test_data/#overview","title":"Overview","text":"<p>The system runs a Multi-Database Architecture simulating a real-world manufacturing environment.</p> Metrics Value Total Datasources 5 (4 Unique Engines) Total Tables 17 Total Rows ~30,000+"},{"location":"reference/test_data/#1-reference-data-manufacturing_ref","title":"1. Reference Data (<code>manufacturing_ref</code>)","text":"<p>Engine: SQLite File: <code>data/manufacturing.db</code> Description: Static reference data used across the organization.</p>"},{"location":"reference/test_data/#tables","title":"Tables","text":""},{"location":"reference/test_data/#factories","title":"<code>factories</code>","text":"<p>Manufacturing plant locations.</p> Column Type Constraints <code>id</code> INTEGER PRIMARY KEY <code>name</code> TEXT NOT NULL <code>location</code> TEXT NOT NULL <code>opened_on</code> DATE NOT NULL <code>capacity_index</code> INTEGER DEFAULT 100"},{"location":"reference/test_data/#machine_types","title":"<code>machine_types</code>","text":"<p>Specifications for equipment models.</p> Column Type Constraints <code>id</code> INTEGER PRIMARY KEY <code>model_name</code> TEXT NOT NULL <code>manufacturer</code> TEXT NOT NULL <code>specifications</code> JSON"},{"location":"reference/test_data/#shifts","title":"<code>shifts</code>","text":"<p>Standard work shifts.</p> Column Type Constraints <code>id</code> INTEGER PRIMARY KEY <code>name</code> TEXT NOT NULL <code>start_time</code> TIME NOT NULL <code>end_time</code> TIME NOT NULL <p>Note: The datasource <code>manufacturing_sqlite</code> also points to this same database file but is used for \"Generic\" demo queries.</p>"},{"location":"reference/test_data/#2-operations-data-manufacturing_ops","title":"2. Operations Data (<code>manufacturing_ops</code>)","text":"<p>Engine: PostgreSQL Description: High-velocity operational data. Tracks daily plant activities.</p>"},{"location":"reference/test_data/#tables_1","title":"Tables","text":""},{"location":"reference/test_data/#employees","title":"<code>employees</code>","text":"<p>Staff details and roles.</p> Column Type Constraints <code>id</code> SERIAL PRIMARY KEY <code>full_name</code> TEXT NOT NULL <code>role</code> TEXT NOT NULL <code>factory_id</code> INTEGER NOT NULL (Logical FK to <code>factories.id</code>) <code>hired_date</code> DATE NOT NULL <code>contact_info</code> JSONB"},{"location":"reference/test_data/#machines","title":"<code>machines</code>","text":"<p>Active equipment assets.</p> Column Type Constraints <code>id</code> SERIAL PRIMARY KEY <code>factory_id</code> INTEGER NOT NULL (Logical FK to <code>factories.id</code>) <code>machine_type_id</code> INTEGER NOT NULL (Logical FK to <code>machine_types.id</code>) <code>name</code> TEXT NOT NULL <code>serial_number</code> TEXT NOT NULL UNIQUE <code>commissioned_on</code> DATE NOT NULL <code>status</code> TEXT NOT NULL DEFAULT 'Active'"},{"location":"reference/test_data/#spare_parts","title":"<code>spare_parts</code>","text":"<p>Inventory of repair parts.</p> Column Type Constraints <code>id</code> SERIAL PRIMARY KEY <code>name</code> TEXT NOT NULL <code>machine_type_id</code> INTEGER NOT NULL (Logical FK to <code>machine_types.id</code>) <code>stock_quantity</code> INTEGER NOT NULL <code>criticality</code> TEXT NOT NULL"},{"location":"reference/test_data/#maintenance_logs","title":"<code>maintenance_logs</code>","text":"<p>History of repairs and downtime.</p> Column Type Constraints <code>id</code> SERIAL PRIMARY KEY <code>machine_id</code> INTEGER NOT NULL (FK to <code>machines.id</code>) <code>performed_at</code> TIMESTAMP NOT NULL <code>maintenance_type</code> TEXT NOT NULL <code>downtime_minutes</code> INTEGER NOT NULL <code>performed_by_id</code> INTEGER NOT NULL (FK to <code>employees.id</code>) <code>notes</code> TEXT"},{"location":"reference/test_data/#3-supply-chain-manufacturing_supply","title":"3. Supply Chain (<code>manufacturing_supply</code>)","text":"<p>Engine: MySQL Description: External-facing supply chain and inventory management.</p>"},{"location":"reference/test_data/#tables_2","title":"Tables","text":""},{"location":"reference/test_data/#suppliers","title":"<code>suppliers</code>","text":"<p>Implementation vendors/partners.</p> Column Type Constraints <code>id</code> INT AUTO_INCREMENT PRIMARY KEY <code>name</code> TEXT NOT NULL <code>contact_email</code> TEXT NOT NULL <code>country</code> TEXT NOT NULL <code>rating</code> FLOAT"},{"location":"reference/test_data/#products","title":"<code>products</code>","text":"<p>Catalog of items produced/bought.</p> Column Type Constraints <code>id</code> INT AUTO_INCREMENT PRIMARY KEY <code>sku</code> VARCHAR(255) NOT NULL UNIQUE <code>name</code> TEXT NOT NULL <code>category</code> TEXT NOT NULL <code>unit_price</code> DECIMAL(10, 2) NOT NULL <code>supplier_id</code> INT NOT NULL (FK to <code>suppliers.id</code>) <code>description</code> TEXT"},{"location":"reference/test_data/#inventory","title":"<code>inventory</code>","text":"<p>Stock levels by warehouse.</p> Column Type Constraints <code>id</code> INT AUTO_INCREMENT PRIMARY KEY <code>product_id</code> INT NOT NULL (FK to <code>products.id</code>) <code>warehouse_location</code> TEXT NOT NULL <code>quantity_on_hand</code> INT NOT NULL <code>last_updated</code> TIMESTAMP DEFAULT CURRENT_TIMESTAMP"},{"location":"reference/test_data/#purchase_orders","title":"<code>purchase_orders</code>","text":"<p>Procurement records.</p> Column Type Constraints <code>id</code> INT AUTO_INCREMENT PRIMARY KEY <code>supplier_id</code> INT NOT NULL (FK to <code>suppliers.id</code>) <code>ordered_at</code> DATETIME NOT NULL <code>status</code> VARCHAR(50) NOT NULL <code>total_amount</code> DECIMAL(12, 2) NOT NULL"},{"location":"reference/test_data/#purchase_order_items","title":"<code>purchase_order_items</code>","text":"<p>Line items for POs.</p> Column Type Constraints <code>id</code> INT AUTO_INCREMENT PRIMARY KEY <code>purchase_order_id</code> INT NOT NULL (FK to <code>purchase_orders.id</code>) <code>product_id</code> INT NOT NULL (FK to <code>products.id</code>) <code>quantity</code> INT NOT NULL <code>unit_price</code> DECIMAL(10, 2) NOT NULL"},{"location":"reference/test_data/#4-historical-data-manufacturing_history","title":"4. Historical Data (<code>manufacturing_history</code>)","text":"<p>Engine: Microsoft SQL Server (MSSQL) Description: Long-term archival of sales and production performance.</p>"},{"location":"reference/test_data/#tables_3","title":"Tables","text":""},{"location":"reference/test_data/#customers","title":"<code>customers</code>","text":"<p>Client database.</p> Column Type Constraints <code>id</code> INT IDENTITY(1,1) PRIMARY KEY <code>company_name</code> NVARCHAR(255) NOT NULL <code>contact_name</code> NVARCHAR(255) NOT NULL <code>email</code> NVARCHAR(255) NOT NULL <code>region</code> NVARCHAR(100) NOT NULL"},{"location":"reference/test_data/#production_runs","title":"<code>production_runs</code>","text":"<p>Completed manufacturing batches.</p> Column Type Constraints <code>id</code> INT IDENTITY(1,1) PRIMARY KEY <code>product_id</code> INT NOT NULL (Logical FK) <code>machine_id</code> INT NOT NULL (Logical FK) <code>start_time</code> DATETIME NOT NULL <code>end_time</code> DATETIME NOT NULL <code>quantity_produced</code> INT NOT NULL <code>scrap_count</code> INT NOT NULL DEFAULT 0"},{"location":"reference/test_data/#defects","title":"<code>defects</code>","text":"<p>QC failures recorded.</p> Column Type Constraints <code>id</code> INT IDENTITY(1,1) PRIMARY KEY <code>production_run_id</code> INT NOT NULL (FK to <code>production_runs.id</code>) <code>defect_type</code> NVARCHAR(100) NOT NULL <code>severity</code> NVARCHAR(50) NOT NULL <code>count</code> INT NOT NULL"},{"location":"reference/test_data/#sales_orders","title":"<code>sales_orders</code>","text":"<p>Validated customer orders.</p> Column Type Constraints <code>id</code> INT IDENTITY(1,1) PRIMARY KEY <code>customer_id</code> INT NOT NULL (FK to <code>customers.id</code>) <code>order_date</code> DATETIME NOT NULL <code>status</code> NVARCHAR(50) NOT NULL <code>total_amount</code> DECIMAL(12, 2) NOT NULL"},{"location":"reference/test_data/#sales_order_items","title":"<code>sales_order_items</code>","text":"<p>Line items for Sales Orders.</p> Column Type Constraints <code>id</code> INT IDENTITY(1,1) PRIMARY KEY <code>sales_order_id</code> INT NOT NULL (FK to <code>sales_orders.id</code>) <code>product_id</code> INT NOT NULL (Logical FK) <code>quantity</code> INT NOT NULL <code>unit_price</code> DECIMAL(10, 2) NOT NULL"},{"location":"reference/test_data/#cross-database-context","title":"Cross-Database Context","text":"<p>The system effectively joins data across these silos logically:</p> <ul> <li>Ops <code>factory_id</code> -&gt; Ref <code>factories.id</code></li> <li>Supply <code>product_id</code> &lt;-&gt; History <code>product_id</code> (Shared SKU logical link)</li> </ul>"},{"location":"reference/api/adapter_sdk/","title":"Adapter SDK","text":"<p>The <code>nl2sql-adapter-sdk</code> defines the contract for new plugins.</p>"},{"location":"reference/api/adapter_sdk/#models","title":"Models","text":""},{"location":"reference/api/adapter_sdk/#datasourceadapter-abstract-base-class","title":"<code>DatasourceAdapter</code> (Abstract Base Class)","text":"<p>The main entry point. Implementations must provide:</p> <ul> <li><code>fetch_schema()</code></li> <li><code>execute(sql)</code></li> <li><code>cost_estimate(sql)</code></li> </ul>"},{"location":"reference/api/adapter_sdk/#table","title":"<code>Table</code>","text":"<p>Schema definition.</p> <pre><code>name: str\ncolumns: List[Column]\nforeign_keys: List[ForeignKey]\n</code></pre> <p>Flags that control the <code>GeneratorNode</code>.</p> Flag Description <code>supports_limit_offset</code> Generator will use LIMIT/OFFSET syntax. <code>supports_dry_run</code> PhysicalValidator will attempt <code>dry_run()</code>. <code>supports_cost_estimation</code> PhysicalValidator will check row counts."},{"location":"reference/api/graph_state/","title":"GraphState API","text":"<p>The <code>GraphState</code> object represents the shared memory passed between nodes in the LangGraph pipeline.</p>"},{"location":"reference/api/graph_state/#attributes","title":"Attributes","text":"Field Type Description <code>user_query</code> <code>str</code> Canonical user query. <code>sql_draft</code> <code>str</code> The Generated SQL string (from Generator). <code>plan</code> <code>PlanModel</code> The AST produced by the Planner. <code>relevant_tables</code> <code>List[Table]</code> subset of schema found by Vector Search. <code>user_context</code> <code>Dict</code> User identity (<code>role</code>, <code>allowed_tables</code>) for Policy Logic. <code>errors</code> <code>List[PipelineError]</code> Accumulating list of errors (Validation/Execution). <code>reasoning</code> <code>List[Dict]</code> Log of \"thoughts\" from agents. <code>execution</code> <code>ExecutionModel</code> Final database results. <code>sub_queries</code> <code>List[SubQuery]</code> If Map-Reduce was triggered."}]}