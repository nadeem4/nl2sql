[pytest]
testpaths =
    packages/core/tests
    packages/adapters/tests
    packages/adapter-sqlalchemy/tests
    packages/cli/tests
    packages/api/tests
addopts = -ra
markers =
    e2e: end-to-end pipeline tests
    integration: requires a live database, vector store and OPENAI_API_KEY