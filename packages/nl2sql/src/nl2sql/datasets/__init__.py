"""Datasets vendored in the wheel, shared by the demo and evaluation.

``chinook.sqlite`` is the Chinook sample database (MIT; see
``THIRD_PARTY_NOTICES.md``). ``support.sqlite`` and ``webanalytics.sqlite`` are
synthetic, generated by ``scripts/generate_demo_datasets.py`` in this
repository and sharing Chinook's customer identities; no third-party data is
involved in either. ``README.md`` describes the tables and how to regenerate
them.

``nl2sql demo`` copies all three into the demo project, and the gold evaluation
dataset reads Chinook directly.
"""
from pathlib import Path

_HERE = Path(__file__).resolve().parent

CHINOOK_DB_PATH = _HERE / "chinook.sqlite"
SUPPORT_DB_PATH = _HERE / "support.sqlite"
WEBANALYTICS_DB_PATH = _HERE / "webanalytics.sqlite"

__all__ = ["CHINOOK_DB_PATH", "SUPPORT_DB_PATH", "WEBANALYTICS_DB_PATH"]
