# Tier 2 baselines

Committed tier 2 scoreboards that `nl2sql benchmark --tier 2 --baseline <file>`
compares a new run against. None is committed yet: the first one comes from a
real run by the owner. See `docs/testing/evaluation-dataset.md`, "Tier 2".
