profiles:
  - id: "sqlite_inventory"
    type: "sqlite"
    connection_string: "sqlite:///inventory.db"
    description: "Local inventory database"

  - id: "postgres_ops"
    type: "postgres"
    connection_string: "postgresql+psycopg2://user:password@localhost:5432/manufacturing_ops"
    description: "Manufacturing Operations (Docker)"
