- id: manufacturing_ref
  description: "Reference data including factories, shifts, and machine types."
  engine: sqlite
  sqlalchemy_url: sqlite:///data/manufacturing.db
  auth: null
  read_only_role: null
  statement_timeout_ms: 8000
  row_limit: 100
  max_bytes: 10485760
  tags: {env: dev, domain: reference}
  feature_flags:
    allow_generate_writes: false
    allow_cross_db: true
    supports_dry_run: false
    supports_estimated_cost: false
    sample_rows_enabled: true

- id: manufacturing_ops
  description: "Operations data including machines, maintenance logs, employees, and spare parts."
  engine: postgres
  sqlalchemy_url: postgresql+psycopg2://user:password@localhost:5432/manufacturing_ops
  auth: null
  read_only_role: null
  statement_timeout_ms: 8000
  row_limit: 100
  max_bytes: 10485760
  tags: {env: dev, domain: operations}
  feature_flags:
    allow_generate_writes: false
    allow_cross_db: true
    supports_dry_run: true
    supports_estimated_cost: true
    sample_rows_enabled: true

- id: manufacturing_supply
  description: "Supply chain data including products, inventory, suppliers, and purchase orders."
  engine: mysql
  sqlalchemy_url: mysql+pymysql://user:password@localhost:3306/manufacturing_supply
  auth: null
  read_only_role: null
  statement_timeout_ms: 8000
  row_limit: 100
  max_bytes: 10485760
  tags: {env: dev, domain: supply_chain}
  feature_flags:
    allow_generate_writes: false
    allow_cross_db: true
    supports_dry_run: false
    supports_estimated_cost: false
    sample_rows_enabled: true

- id: manufacturing_history
  description: "Historical data including production runs, defects, customers, and sales orders."
  engine: mssql
  sqlalchemy_url: "mssql+pyodbc://sa:StrongPass2023!@127.0.0.1:1433/manufacturing_history?driver=ODBC+Driver+17+for+SQL+Server&TrustServerCertificate=yes"
  auth: null
  read_only_role: null
  statement_timeout_ms: 8000
  row_limit: 100
  max_bytes: 10485760
  tags: {env: dev, domain: history}
  feature_flags:
    allow_generate_writes: false
    allow_cross_db: true
    supports_dry_run: false
    supports_estimated_cost: false
    sample_rows_enabled: true
